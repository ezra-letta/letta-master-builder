# Runtime Claims

This directory stores target-, version-, topology-, environment-, date-, and operation-scoped runtime observations only after separate authorization. Revision 0.4 contains no runtime claim and does not authorize a canary.
