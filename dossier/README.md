# Normalized Design Dossier

The curriculum accumulates one normalized dossier rather than many overlapping plans. `template.yml` defines the canonical record. Each module owns particular fields or adds evidence-backed deltas; later modules must not silently rewrite earlier decisions.

The dossier is a design artifact. Completing it does not execute a controller, create an agent, validate a runtime, certify a learner, or qualify a deployment.
