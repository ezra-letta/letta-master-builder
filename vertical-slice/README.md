# Revision 0.4 Vertical Slice

This directory is maintainer infrastructure, not mandatory curriculum content and not part of bare-pointer execution. It proves one path through evidence registration, safe-subset API design, exact-package TypeScript compilation, a labeled project fixture, partial-success recovery, and non-claim discipline.

The lock file pins the full compile dependency closure. Installation and compilation require separate maintainer authorization and do not call Letta APIs or models.
